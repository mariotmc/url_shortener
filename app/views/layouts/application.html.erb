<!DOCTYPE html>
<html>
  <head>
    <title>UrlShortener</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://unpkg.com/@popperjs/core@2"></script>
    <script src="https://unpkg.com/tippy.js@6"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="prose mx-auto mt-28 px-5">
      <% if alert %>
        <p class="flex items-center gap-2 w-fit bg-yellow-100 text-yellow-800 text-sm font-medium px-4 py-2 rounded">
          <%= alert %>
        </p>
      <% end %>

      <% if notice %>
        <p class="flex items-center gap-2 w-fit bg-blue-100 text-blue-800 text-sm font-medium px-4 py-2 rounded">
          <%= notice %>
        </p>
      <% end %>

      <nav class="flex items-center justify-between mb-4">
        <h1 class="mb-0 text-2xl font-bold"> <%= link_to "URL Shortener", root_path, class: "font-extrabold no-underline" %></h1>

        <div class="flex items-center gap-4">
          <% if user_signed_in? %>
            <%= link_to "Profile", edit_user_registration_path, class: "text-blue-500 hover:text-blue-700" %>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "text-blue-500 hover:text-blue-700" %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "text-blue-500 hover:text-blue-700" %>
            <%= link_to "Log in", new_user_session_path, class: "text-blue-500 hover:text-blue-700" %>
          <% end %>
        </div>
      </nav>

      <%= yield %>
    </main>
  </body>
</html>
