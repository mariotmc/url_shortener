<%= form_with model: link do |f| %>
  <div class="flex items-center gap-2">
    <%= f.url_field :url, required: true, autofocus: true, placeholder: "https://example.com", class: "flex-grow rounded" %>
    <%= f.submit "Save", class: "px-3 py-2 bg-gray-100 rounded" %>
  </div>
<% end %>
