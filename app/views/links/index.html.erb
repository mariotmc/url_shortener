<%= render partial: "form", locals: { link: @link } %>

<div id="links" class="my-8 flex flex-col justify-center divide-y divide-gray-100">
	<%= render @links %>

	<% if @pagy.pages > 1 %>
		<div class="py-10 text-center">
			<%== pagy_nav(@pagy) %>
		</div>
	<% end %>
</div>
